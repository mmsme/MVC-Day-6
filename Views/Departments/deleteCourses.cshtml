@model List<MVC_Day_6.Models.Course>
@{
    ViewBag.Title = "deleteCourses";
}

<h2>Remove Courses From @ViewBag.dept.DeptName</h2>
<form method="post">
    <table class="table">
        <tr class="thead-dark">
            <th>Course Name</th>
            <th>Remove</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.CourseName</td>
                <td>@Html.CheckBox("crs." + item.CourseId.ToString())</td>
            </tr>
        }
    </table>
    <input type="submit" class="btn btn-warning" value="Remove" />
    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-danger" })
</form>

